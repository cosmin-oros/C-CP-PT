#include <stdio.h>
#include <stdlib.h>

unsigned long int fib(unsigned long int n){
    // long int f[n+1];

}

int main(void){
    printf("fib(10) = %lu\n",fib(10));
    printf("fib(3) = %lu\n",fib(3));
    printf("fib(5) = %lu\n",fib(5));
    printf("fib(7) = %lu\n",fib(7));

    return 0;
}