#include <stdio.h>
#include <string.h>

/*
3. Span of characters Implement the functions (a) strspn, (b) strcspn. (Read class notes, man page or standard for specifications).
Then, implement a function size_t pieces(const char *s, const char *allow) which counts how many separate substrings containing characters from allow are to be found in s.
*/

int main(){
    
    return 0;
}