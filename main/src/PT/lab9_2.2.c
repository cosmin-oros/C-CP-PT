#include <stdio.h>
/*
2. Filtering input Write a function that takes as parameter a 32-bit integer representing a set of 
lowercase characters (one bit per letter, bit 0 for 'a'), and filters text read from input until EOF, 
printing only those lowercase letters that are not in the given set, and any characters which are not lowercase letters. 
Use the function to filter out any lowercase vowels from input.
*/
int main(){
    
    return 0;
}